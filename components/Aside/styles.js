import styled from "styled-components";

/* sidebar */
export const StyledAside = styled.aside`
  .sidebar-categories {
    list-style-type: none;
    padding-right: 4em;
  }
  .side-bar-item:hover {
    cursor: pointer;
  }
`;
